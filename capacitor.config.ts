import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kenai.app',
  appName: 'KenaiStreaming',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
